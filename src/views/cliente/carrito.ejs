<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carrito</title>
    <link rel="stylesheet" href="/public/cliente/style.css">
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        th,
        td {
            padding: 12px;
            text-align: center;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #0F0C29;
            color: #fff;
            font-size: 1.2rem;
        }

        td {
            font-size: 1rem;
        }

        button {
            background-color: #0F0C29;
            color: white;
            padding: 12px 20px;
            font-size: 1rem;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #4f91ff;
        }

        p {
            font-size: 1.2rem;
            color: #333;
        }

        table td:last-child {
            font-weight: bold;
        }

        .empty-cart-message {
            text-align: center;
            font-size: 1.5rem;
            color: #777;
        }
    </style>
</head>

<body>
    <%- include('../partials/navCliente') %>
        <h1>Tu Carrito</h1>

        <% if (productosCarrito.length===0) { %>
            <p>Tu carrito está vacío.</p>
            <% } else { %>
                <table>
                    <thead>
                        <tr>
                            <th>Nombre</th>
                            <th>Cantidad</th>
                            <th>Precio Unitario</th>
                            <th>Total</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% productosCarrito.forEach(producto=> { %>
                            <tr>
                                <td>
                                    <%= producto.nombre %>
                                </td>
                                <td>
                                    <%= producto.cantidad %>
                                </td>
                                <td>
                                    S/<%= producto.precio_unitario %>
                                </td>
                                <td>
                                    S/<%= (producto.cantidad * producto.precio_unitario) %>
                                </td>
                            </tr>
                            <% }) %>
                    </tbody>
                </table>
                <% } %>

                    <% if (productosCarrito.length> 0) { %>
                        <form action="/cliente/carrito/confirmar" method="POST">
                            <button type="submit">Confirmar Pedido</button>
                        </form>
                        <% } %>
</body>

</html>